## 🐳 Инфраструктура проекта (Docker Compose)

# 🐘 Kafka System (ZooKeeper Mode)

![Go](https://img.shields.io/badge/Go-1.21+-00ADD8?style=flat&logo=go)
![Kafka](https://img.shields.io/badge/Apache_Kafka-ZooKeeper_Mode-231F20?style=flat&logo=apachekafka)
![ZooKeeper](https://img.shields.io/badge/Apache_ZooKeeper-Enabled-C75C5C?style=flat&logo=apache)
![Docker](https://img.shields.io/badge/Docker-Compose-2496ED?style=flat&logo=docker)

Проект разворачивает классическую архитектуру Kafka с использованием **ZooKeeper**. В файле `docker-compose.yml` описано взаимодействие трех ключевых сервисов:

### 1. 🐘 Zookeeper
Сервис координации и управления метаданными кластера.
- Запускается первым.
- Открывает порт `2181` для взаимодействия с брокером Kafka и Kafka UI.
- Критически важен для выбора контроллера и хранения конфигураций топиков.

### 2. 📨 Kafka Broker
Основной узел обработки сообщений.
- **Запуск**: Использует директиву `depends_on`, чтобы стартовать строго после полной инициализации Zookeeper.
- **Режим**: Настроен для работы в режиме Single Broker (фактор репликации 1).
- **Сетевая конфигурация**: Реализована схема двойных слушателей (`LISTENERS`), решающая проблему подключения из разных сетей:
    - **Internal**: Для общения внутри Docker-сети (доступен для Kafka UI).
    - **External**: Для подключения локальных клиентов (Go, CLI) с хост-машины.

### 3. 🖥 Kafka UI
Веб-интерфейс для мониторинга и управления кластером.
- **Запуск**: Стартует последним (`depends_on: kafka`).
- **Связь**: Подключается к брокеру через внутреннюю сеть Docker (по имени контейнера).
- **Доступ**: Пробрасывает порт `8080` наружу, предоставляя удобный дашборд для просмотра топиков, консьюмеров и сообщений.
